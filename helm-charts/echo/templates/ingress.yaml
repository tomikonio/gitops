apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
spec:
  rules:
    - host: shushu.zapto.org
      http:
        paths:
          - path: /{{ .Values.hostname }}
            backend:
              serviceName: {{ .Values.name }}
              servicePort: 80
