apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
spec:
  rules:
    - http:
        paths:
          - path: /{{ .Values.hostname }}
            backend:
              serviceName: {{ .Values.name }}
              servicePort: 80
